import {StyleSheet} from 'react-native';

const CommonSyle = StyleSheet.create({
  stickText: {
    textDecorationLine: 'line-through',
    textDecorationStyle: 'solid',
  },
});

export default CommonSyle;
